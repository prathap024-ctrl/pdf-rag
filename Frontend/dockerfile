# Dockerfile for frontend
FROM node:20-alpine

WORKDIR /app

# Only copy package.json files first to leverage Docker cache
COPY package*.json ./

# Clean install to prevent native mismatch
RUN npm install

# Now copy the rest of the frontend code
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]
